<h2>Authors</h2>

<ul>
{{#each model as |author|}}
  <li>{{author.name}}</li>
  <ul>
    {{#each author.books as |book|}}
      <li>{{book.title}} ({{book.year}})</li>
    {{/each}}
  </ul>
{{/each}}
</ul>
